package com.accounting.app.capture.model

/**
 * 采集来源（P0: ACCESSIBILITY / NOTIFICATION；P1: OCR / SCREENSHOT）
 */
enum class CaptureSource {
    ACCESSIBILITY,
    NOTIFICATION,
    OCR,
    SCREENSHOT
}
