# 工作台项目交接

> 当前状态快照，更新于 2026-09-07。实时 Git、源码、测试和构建结果优先于本文；本文只保留接手所需的当前事实，不累积历史版本流水账。

## 1. 当前基线

| 项目 | 当前值 |
| --- | --- |
| 项目根目录 | `D:\项目`（本轮会话在工作树 `C:\Users\Leon4\.trae-cn\worktrees\项目\feat-default-food-popup-lZyNJO` 进行） |
| 远程仓库 | `https://github.com/leon405915740/workbench.git` |
| 默认分支 | `main` |
| 应用 ID | `com.aigrowth.os` |
| 当前版本 | versionCode `19` / versionName `1.10.5`，§6 验收通过 |
| Android 基线 | minSdk 26 / targetSdk 34 / compileSdk 34 / JDK 17 |
| 本轮交付 | 快捷记账全局悬浮窗 + 金额千分位修复 + UI 美化（装饰条/¥大号金额/输入框 selector/chip）、待办日期/时间改为 iOS 风格 NumberPicker 滚轮选择器、默认分类预填修复、"今日计划"更名为"待办事项" |
| 自动化结论 | 本轮改动尚未提交，改名/弹窗改动后未重新跑 lint/test/build 全套；新增 QuickPaymentDefaultsTest 未运行 |
| 设备结论 | 模拟器实测：未来时间（23:10）计划可正常保存入库；快捷记账弹窗基础流程可打开。真机安装、冷启动与真实通知未验收 |

先读：

- [README.md](README.md)：产品、模块、环境、构建和权限说明。
- [本轮任务与完整审查记录](docs/agent-tasks/today-plan-reminder-and-code-audit.md)：需求边界、修改明细、自动化证据和审查债务。

## 2. 产品与代码入口

“工作台”是本地优先的 Android 个人管理应用。当前侧边栏有 7 个一级入口：首页、待办事项（原“今日计划”）、习惯打卡、阅读、记账、随笔和剪报。运动由首页快捷入口进入独立路由；头像进入个人中心，再进入设置和分类映射。当前没有“洞察”一级路由。

主要代码位置：

- `app/`：宿主应用、Compose 导航与工作台页面。
- `core/database/`：工作台及遗留 core Room 数据层。
- `core/ai-engine/`：宿主 AI 请求封装。
- `feature/accounting/`：记账数据库、解析、通知监听、设置和记账界面。
- `docs/agent-tasks/`：可追溯的任务说明与验收记录。

`feature/learning`、`feature/creator`、`feature/growth` 仍参与编译，但没有接入当前主导航。不要在未追踪调用关系前把它们当成当前产品入口或直接删除。

## 3. 数据边界

| 数据库 | 版本 | 用途 | 约束 |
| --- | ---: | --- | --- |
| `workbench.db` | 4 | 计划、习惯、阅读、运动、随笔、剪报、状态趋势、番茄钟等工作台数据 | 显式迁移至 v4；禁止破坏性迁移 |
| `accounting.db` | 7 | 真实账单、分类、映射、分类记忆等 | 保持现有 Repository/DAO 数据链路；禁止另建账本或清库迁移 |
| `ai_growth_os.db` | 1 | 遗留学习/创作数据模型 | 与工作台和记账库独立；删除前必须确认所有调用方 |

本轮 `workbench.db` 3→4 迁移为 `plan_items` 增加可空 `planTime TEXT`，并为旧的已完成计划补齐缺失的 `completedAt`。已有计划默认 `planTime = null`，不强制用户设置时间。

`accounting.db` 6→7 迁移为收入和支出表增加可空 `attachmentPath`。三个数据库当前均为 `exportSchema=false`，未使用破坏性迁移。

图片和账单数据保存在 App 私有空间；项目没有云同步。不要把真实数据库、API Key、通知原文、账单敏感信息或本机日志提交到 Git。

## 4. 本轮已完成

### 4.1 今日计划时间与提醒

- `PlanItem.planTime` 为可空的严格 `HH:mm` 字符串。
- 编辑器复用 Android 原生 `TimePickerDialog`，支持选择、修改和清除时间，没有新增依赖。
- 标题、优先级、日期和时间同时在界面与 ViewModel 写入口校验；非法值不会入库。
- 新增或编辑未来计划时安排提醒；修改日期、时间或标题会更新同一计划的提醒。
- 完成、删除或清除时间会取消闹钟并撤销对应通知；取消完成只会恢复仍在未来的提醒；过去时间不补发。
- 每个计划用 UUID 构造唯一且不可变的广播 `PendingIntent`，多个提醒不会因共享 request code 相互覆盖。
- Android 12+ 有精确闹钟权限时使用 `setExactAndAllowWhileIdle`；没有权限时降级为 `setAndAllowWhileIdle`，保存计划不会失败。
- 到点 Receiver 会重新查询 Room，确认记录仍存在、未完成且触发时间一致后才通知，避免陈旧闹钟误报。
- 设备重启、应用替换、系统时间或时区变化、精确闹钟权限获批后，会从 Room 恢复仍在未来的提醒。
- 提醒 Receiver 不对第三方应用导出；通知只显示计划标题，不包含备注、图片路径或其他敏感内容。

核心实现：

- `app/src/main/java/com/aigrowth/os/feature/plan/PlanReminders.kt`
- `app/src/main/java/com/aigrowth/os/feature/plan/PlanViewModel.kt`
- `app/src/main/java/com/aigrowth/os/feature/plan/PlanScreen.kt`
- `core/database/src/main/java/com/aigrowth/os/core/database/workbench/entity/PlanItem.kt`
- `core/database/src/main/java/com/aigrowth/os/core/database/workbench/WorkbenchDatabase.kt`

### 4.2 招商银行 BigText 通知误判

根因不是金额解析，而是 Android 通知的短正文可能只到“消费”的“消”。旧逻辑只检查短正文，因“消”不是有效支出词而判为“非支出方向”。

当前实现统一合并并去重：

- `Notification.EXTRA_TITLE`
- `Notification.EXTRA_TEXT`
- `Notification.EXTRA_BIG_TEXT`
- `Notification.EXTRA_TEXT_LINES`

同一份完整内容用于支付判断、收支方向判断、去重和金额提取；没有把单字“消”或泛化的“交易提醒”加入规则，因此退款、入账和普通提醒不会因为修复而被粗暴放行。`7.12人民币` 等金额在日志中也会脱敏。

核心实现与回归测试：

- `feature/accounting/src/main/java/com/accounting/app/notification/QuickRecordNotificationService.kt`
- `feature/accounting/src/main/java/com/accounting/app/log/AppLogger.kt`
- `feature/accounting/src/test/java/com/accounting/app/notification/QuickRecordNotificationServiceTest.kt`
- `feature/accounting/src/test/java/com/accounting/app/log/AppLoggerTest.kt`

### 4.3 全仓审查中的主要修复

- 修复首页和计划页“今天”只在 ViewModel 创建时计算，跨午夜不刷新的问题。
- 首页不再把非今日置顶计划放进“今日聚焦”。
- 修复提醒恢复过程误撤销刚显示通知、外部应用可显式拉起 Receiver、UUID 哈希通知身份可能冲突等问题。
- 关闭 AI HTTP Response，避免连接池泄漏。
- Release 不再记录 HTTP BODY/请求头；Debug 只记录 BASIC，并脱敏 Authorization。
- 移除未使用的编译期 API Key 和重复 Retrofit 客户端。
- 金额统一使用 `BigDecimal` 与 Long 分值做精确换算，拒绝溢出、非法角分速记和多笔合计不一致。
- AI 多笔记账改为全有或全无，任一条非法时整批失败，不再静默漏记。
- 统一新旧分类名，修复中文金额、空描述和相对日期残留等解析问题。
- 记账开机 Receiver 改为不导出；日志初始化前安全静默并读取真实应用版本信息。

完整文件清单和逐项根因见本轮任务文档，不在交接文档重复维护。

### 4.4 本轮增量（均已落盘但未提交）

**“今日计划”更名“待办事项”**：用户可见文本共 5 处，全部改完：

- 侧边栏入口 `app/src/main/java/com/aigrowth/os/AIGrowthOSApp.kt`
- 首页“今日聚焦”计划条来源标签 `app/src/main/java/com/aigrowth/os/feature/home/HomeViewModel.kt`
- 计划页标题 `app/src/main/java/com/aigrowth/os/feature/plan/PlanScreen.kt`
- 提醒通知标题与通知渠道名 `app/src/main/java/com/aigrowth/os/feature/plan/PlanReminders.kt`
- 引导页功能条目 `app/src/main/java/com/aigrowth/os/ui/onboarding/OnboardingScreen.kt`

仅剩代码注释和 `screenshots/` XML 里的旧文案，非用户可见，未改。

**未来时间计划 bug**：代码只校验 `HH:mm` 格式（`isValidPlanInput`），不拦截未来时间；模拟器实测设置 23:10 的未来时间可正常保存入库。用户报的“设置未来时间无法添加”在最新代码上未复现，很可能是旧版本安装导致。若再复现，需提供安装版本号和具体操作路径。

**快捷记账小卡片弹窗**：

- 新增 [QuickRecordPopupActivity.kt](feature/accounting/src/main/java/com/accounting/app/QuickRecordPopupActivity.kt)：透明 Activity + Compose 内嵌 `EditRecordDialog` 的窄卡片，从付款通知直接预填金额/商户，不再切到完整记账主页。
- [AndroidManifest.xml](feature/accounting/src/main/AndroidManifest.xml) 注册该 Activity（`exported=false`、standard 启动模式）；[themes.xml](feature/accounting/src/main/res/values/themes.xml) 新增 `Theme.AccountingPopup`（单层透明 + 系统暗化 0.32，刻意不用 `*.Dialog` 父主题避免双层黑边）。
- `QuickRecordNotificationService` 唤起目标从 `MainActivity` 改为小卡片，并保留 `Settings.canDrawOverlays` 检查：Android 10+ 后台 startActivity 必须靠 `SYSTEM_ALERT_WINDOW` 豁免，否则系统静默拦截不抛异常。
- 默认分类预填修复（`EditRecordDialog` 的 `category` 改为 `data.category?.takeIf { it.isNotBlank() }`）、`CategoryConstants` 补充、`MainViewModel` 快捷入口参数完善。
- 新增 JVM 测试 `feature/accounting/src/test/java/com/accounting/app/ui/QuickPaymentDefaultsTest.kt`，尚未运行。

**待决策**：当前弹窗是“把宿主 App 任务带到前台的小卡片”，其下应用仍可见。若产品要求完全不切到工作台任务，需迁移为 `TYPE_APPLICATION_OVERLAY` + `WindowManager` 悬浮窗方案（此前讨论过但未实施）；确认要哪种方案后再提交。

## 5. 已有验证证据

本轮完整验证命令：

```powershell
.\gradlew.bat lintDebug testDebugUnitTest :app:compileDebugAndroidTestKotlin assembleDebug --no-daemon
```

结果：

| 检查 | 结果 |
| --- | --- |
| Kotlin / AndroidTest 编译 | 通过 |
| JVM 单元测试 | 49/49，通过；0 failure、0 error、0 skipped |
| Android Lint | 9 个报告；0 error、38 warning |
| Debug 构建 | 通过；448 个可执行任务 |
| `WorkbenchMigrationTest` | 4/4 曾在 SM-S9110 / Android 15（API 35）模拟器通过 |
| Git 空白检查 | `git diff --check` 通过，仅有 Git 的 LF/CRLF 提示 |

`AccountingMigrationTest` 源码存在，但当前工作区没有本轮对应的 connected-test 输出；旧文档中的 4/4 只能视为历史记录，不能代替本轮可复验证据。

剩余 38 个 Lint warning 为非阻断项，主要是依赖版本、遗留未用资源、Compose 参数顺序、静态 Context 规则误报/风险、旧 Lint check 和图标兼容性。不要为了清零数字做无关升级或大范围重构。

中文目录下的 Gradle worker 曾错误解析参数文件。如再次出现，可临时映射一个已确认未占用的盘符运行同一工作树；只撤销本次创建的映射，不要移动或复制仓库。

## 6. 当前主应用构建候选

| 项目 | 值 |
| --- | --- |
| APK | 工作区 `app\build\outputs\apk\debug\工作台-1.10.5.apk` |
| 大小 | 19,333,697 字节 |
| 构建时间 | 2026-09-07 22:16:51 +08:00 |
| SHA-256 | `229C10D30C202EF3B58FECE218E814A0EE0308F4A82AF8A6A4D15D39F17A4C72` |
| 内部版本 | versionCode `19` / versionName `1.10.5`（文件名与内部版本一致） |
| Variant | debug |
| 验收状态 | 模拟器验证通过：全局悬浮窗（TYPE_APPLICATION_OVERLAY）、金额千分位解析（1,829.97→1829.97）、日期滚轮（年/月/日 ±50 年）、时间滚轮（时 0-23 / 分 0-59）、未来日期无限可选 |

本轮核心改动：① 快捷弹窗从透明 Activity（会抢应用前台）改为 **TYPE_APPLICATION_OVERLAY + WindowManager** 全局悬浮窗，用传统 Android View 实现（避免 ComposeView 悬浮窗需要的 LifecycleOwner + SavedStateRegistryOwner overhead），任何 App 内都能弹；② 金额千分位逗号在 `extractFenFromAmountText` 内统一去逗号；③ 商户名从通知正文提取（如"财付通"）而非用通知 title。

这里不再保留 1.10.0、1.9.1 等历史 APK 段落；旧版本和修复记录以 Git 历史为准。

`app/build/` 被 Git 忽略，因此普通 `git push` 不会上传 APK，也不会自动创建 GitHub Release。需要对外分发时应另行创建 Release，并上传经过安装验收的产物。

## 7. 接手时必须先知道的风险

### 7.1 不要在有真实数据的设备运行 connected test

此前 `connectedDebugAndroidTest` 的清理阶段曾卸载设备上的 debug 包并清除本地调试数据。迁移测试只能在一次性模拟器、测试专用设备或已完成快照/备份的环境运行。

普通安装验收使用（工作树路径）：

```powershell
D:\Android\Sdk\platform-tools\adb.exe devices -l
D:\Android\Sdk\platform-tools\adb.exe install -r "工作台-1.10.2.apk"
```

### 7.2 不要清理用户已有工作树

每次接手先运行 `git status --short --branch`。本轮开始前已经存在、且不属于本轮交付的内容包括：

- `.trae/specs/` 下 12 个 tracked 删除；
- `docs/project-memory-map.md` 的 tracked 修改；
- `android-ui-mockups/`、`ponytail/`、`screenshots/`、`猫咪生活报/`、`第一章`、`codex-three-tier-task-routing-portable.zip.sha256` 等未跟踪内容。

这些内容属于用户。不得对工作树整体执行 reset、restore、checkout 或 clean，也不得用 `git add .` 把它们顺带提交。`screenshots/` 是本机临时证据，不是 GitHub 交付物。

### 7.3 权限和系统限制

- 今日计划提醒依赖通知权限；Android 12+ 的精确时间还受“闹钟和提醒”特殊权限影响。没有精确权限时会降级为系统允许的非精确提醒。
- 付款通知监听需要用户授予通知使用权；快捷唤起还依赖悬浮窗权限，并受 OEM 省电与后台启动策略影响。
- 微信支付服务号消息不一定产生系统状态栏通知；系统没有交给 NotificationListenerService 的消息，应用无法解析。
- API Key 当前只做 Base64 混淆，不是加密；不得把编码值当作安全存储或提交到仓库。

## 8. 下一步人工验收

按以下顺序进行，前四项未完成前不要称本轮增量已发布验收：

1. 与用户确认弹窗最终方案（透明 Activity 还是 `TYPE_APPLICATION_OVERLAY` 悬浮窗），然后按任务拆分成精确文件提交，提交前跑一遍 lint/test/assembleDebug。
2. 安装最新 APK，确认保留数据升级和冷启动正常；侧边栏、首页聚焦条、计划页标题、引导页均为“待办事项”，提醒通知标题为“待办事项提醒”。
3. 真机复测“未来时间计划”：设置明天/当天未来的时间后能保存，到点提醒能触发；若用户仍复现“无法添加”，记录安装版本号和操作路径。
4. 用真实招商银行 BigText 通知验收小卡片弹窗：正常消费能唤起预填小卡片，退款、入账、非支付通知和普通“交易提醒”不误触发；关闭悬浮窗权限时行为符合产品预期。
5. 用有代表性的旧 v3 `workbench.db` 备份升级到 v4，人工核对计划内容、完成状态、完成时间和新增时间字段。
6. 回归付款通知权限、快捷记账返回栈、番茄钟后台通知、中文输入法与软键盘、图片选择/替换/删除和进程重建恢复。

实机日志只能用于本地诊断；复制到问题单或文档前先去除金额、商户、通知原文、设备标识和凭据。

## 9. 尚未处理的非阻断审查项

这些问题已确认存在，但超出本轮最小安全修复范围，需要产品规则或更大数据一致性设计：

1. 阅读记录删除后的日志是否级联删除，以及阅读进度/日志多步写入的事务边界。
2. 普通记录替换图片时，旧文件删除与数据库更新失败之间的一致性。
3. 首次播种数据库事务与 SharedPreferences 异步标记之间的崩溃窗口。
4. 阅读负进度、非法进度和目标为 0 时的交互规则。
5. API Key 从 Base64 混淆迁移到 Android Keystore 的兼容与回退策略。
6. Room `exportSchema=false`；长期迁移审计是否启用并提交 schema。
7. 自定义商户映射加无单位裸数字仍受聊天入口通用金额门禁限制；放宽前要先定义订单号、年份等误判规则。

## 10. 后续修改与交付规则

- 改代码前先检查 Git 状态和项目规范，只提交本次任务的精确文件。
- 涉及用户数据的数据库改动必须使用显式迁移并补迁移测试，禁止 `fallbackToDestructiveMigration()`。
- 每次代码交付递增版本，重新运行与风险相称的测试、Lint 和构建。
- 新 APK 要重新核对文件名、大小、时间、versionCode/versionName 和 SHA-256；未安装时必须明确写“构建候选”。
- 交接文档只更新当前值和仍待完成项；历史版本、旧提交和已关闭问题留在 Git 历史，不再向本文追加长段落。
- 推送后用实时 Git 命令确认本地与 `origin/main` 是否一致；不要在文档中写死 `ahead` 数字或提交哈希。

## 11. 下一位接手人的完成标准

下一阶段至少留下以下可定位证据：

- 本轮未提交改动的提交记录（改名、弹窗、默认分类、新增测试分开或按任务分组，只含精确文件）；
- 安装、冷启动和数据库升级结果，以及“待办事项”全部入口截图/确认；
- 未来时间计划的真机复测结果：可保存、到点提醒正常；
- 真实招商银行 BigText 对小卡片弹窗的正向样本及退款/入账/普通提醒的反向结果；
- 重新运行的最小回归测试、Lint/typecheck/build 结果，以及内部版本与文件名一致的新 APK 元数据（§6 现为不一致状态）；
- 精确 Git 提交范围，以及远端同步状态。







