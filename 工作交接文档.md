# 工作交接文档：记账 App 整合 AI-Growth-OS 工作台

| 项目 | 内容 |
|---|---|
| 文档版本 | v2.0 |
| 交接日期 | 2026-08-28 |
| 项目真源 | `D:\记账app\工作台app\AI-Growth-OS` |
| 文档性质 | 当前正式交接文档；旧 README、overview、RELEASE_CHECKLIST 和旧版交接文档仅作历史参考，可能过时 |

> 本文取代 v1.0《工作交接文档》及《交接文档_工作台记账模块.md》。当前状态以源码、构建产物和设备实测结果为准。

## 一、项目定位与当前入口

这是一个“简化、好用、不要过度设计”的 Android 极简工作台，将记账 App 与三项日常记录功能整合在一起。

| 入口 | 当前实现 | 状态 |
|---|---|---|
| 健身打卡 | `feature/simple/CheckInScreen.kt`：今日切换、连续天数 | 源码已完成，待设备验收 |
| 自媒体 | `feature/simple/MediaScreen.kt`：记录增改删、勾选 | 源码已完成，待设备验收 |
| 学英语 | `feature/simple/EnglishScreen.kt`：记录增改删、勾选 | 源码已完成，待设备验收 |
| 记账 | 工作台页内复用 `MainScreen`，含聊天、手动记账、统计 | 已接入，待设备验收 |
| 设置 | 抽屉底部入口：自动学习、快捷记账/通知监听权限、记忆分类管理、CSV/日志导出 | 已精简，待设备验收 |

导航为四个主入口（健身打卡/自媒体/学英语/记账）加抽屉底部设置，`startDestination=CheckIn`；`MemoryMapping` 是隐藏的记账/设置子路由，不是主入口。四个主屏和设置页均有汉堡按钮。

## 二、当前完成状态

| 项目 | 状态 | 说明 |
|---|---|---|
| 核心极简整合 | 已完成 | 导航、三页、记账内嵌、设置精简、启动流程已落地 |
| Debug 编译 | 已验证成功 | v1.0 所述 SettingsScreen 参数编译阻断已解决 |
| APK | 已生成 | 具体信息见第五节 |
| 设备行为 | 未验证 | 未宣称真机/模拟器点击、重启、通知支付端到端已完成 |
| 发布准备 | 未完成 | 当前只有 debug 包；签名、混淆和正式发布待后续 |

### 2.1 关键实现

- 记账在工作台页内通过 `ViewModelProvider` + `MainViewModel.factory(AccountingApp.getInstance().appRepository)` 获取 ViewModel，再复用 `MainScreen`。
- `QuickRecordNotificationService` 仍会启动 `com.accounting.app.MainActivity`，通知触发时可能另开 Activity，必须真机确认返回栈和体验。
- 三页使用 Preferences DataStore，存储名 `simple_workbench`。健身支持今日状态和连续天数；自媒体、英语支持增改删和勾选。
- Settings 已移除 AI 卡片；通知权限状态在生命周期恢复时刷新。
- Splash 与 Onboarding 的 AI 配置均可跳过。新 API Key 以 `b64:` + Base64 存储并兼容旧明文；Base64 只是混淆，不是加密。

## 三、架构、模块与关键文件

### 3.1 工程参数

- Kotlin + Jetpack Compose；Navigation Compose 2.7.7；Room 用于记账数据层。
- 宿主使用 Hilt；内嵌记账页使用手动 `ViewModelProvider`。
- `compileSdk/targetSdk=34`，`minSdk=26`，JVM 17。
- `versionCode=6`，`versionName=1.5`；DataStore 依赖 `androidx.datastore:datastore-preferences:1.0.0`。
- 数据库未使用 `fallbackToDestructiveMigration()`；Manifest 中 `allowBackup=true`。

### 3.2 模块与包名

| 模块/标识 | 说明 |
|---|---|
| `app` | 工作台宿主：启动、导航、设置、三页极简功能 |
| `core:database` / `core:network` / `core:ai-engine` / `core:design` | 基础能力 |
| `feature:accounting` | 记账模块，包名 `com.accounting.app` |
| `feature:learning` / `feature:growth` / `feature:creator` | 历史功能文件保留，当前主导航不依赖深层入口 |
| 宿主 applicationId | `com.aigrowth.os` |

两套包名不可混淆。独立旧项目 `D:\记账app\app\` 本轮未修改，不是本次工作台真源。

### 3.3 关键文件

| 文件 | 作用 |
|---|---|
| `app/src/main/java/com/aigrowth/os/AIGrowthOSApp.kt` | 导航、抽屉、NavHost、主入口和设置路由 |
| `app/src/main/java/com/aigrowth/os/MainActivity.kt` | Splash、Onboarding、工作台启动 |
| `app/src/main/java/com/aigrowth/os/AIGrowthOSApplication.kt` | Hilt Application；初始化 `AccountingApp` |
| `app/src/main/java/com/aigrowth/os/feature/simple/{SimpleDataStore,CheckInScreen,MediaScreen,EnglishScreen}.kt` | 三页及 `simple_workbench` 本地存储 |
| `app/src/main/java/com/aigrowth/os/feature/settings/SettingsScreen.kt` | 设置与导出 |
| `feature/accounting/.../ui/MainViewModel.kt` | `MainViewModel.factory(repository)` |
| `feature/accounting/.../MainActivity.kt` | 可复用 `MainScreen(viewModel)` |
| `feature/accounting/.../AccountingApp.kt` | 运行时单例和仓储 |
| `feature/accounting/.../notification/QuickRecordNotificationService.kt` | 付款通知识别与唤起 |

## 四、本轮源码/配置变更记录

以下 12 个文件来自本轮实施记录。由于整个 `AI-Growth-OS` 在顶层 Git 中为 untracked，这不是可靠的 Git diff 清单：

1. `app/build.gradle.kts`
2. `AIGrowthOSApp.kt`
3. `SettingsScreen.kt`
4. `feature/simple/CheckInScreen.kt`
5. `feature/simple/EnglishScreen.kt`
6. `feature/simple/MediaScreen.kt`
7. `feature/simple/SimpleDataStore.kt`
8. `MainActivity.kt`
9. `OnboardingScreen.kt`
10. `SplashScreen.kt`
11. `core/ai-engine/ApiKeyService.kt`
12. `core/database/di/DatabaseModule.kt`

独立旧项目 `D:\记账app\app\` 未修改。

## 五、已验证事项与 APK 信息

2026-08-27 在 `D:\记账app\工作台app\AI-Growth-OS` 执行：

```powershell
$env:TEMP='D:\记账app\jtmp'; $env:TMP='D:\记账app\jtmp'; .\gradlew.bat assembleDebug --no-daemon --console=plain
```

结果：`BUILD SUCCESSFUL in 3m 49s`；299 个 actionable tasks（58 executed、241 up-to-date）。临时 `D:\记账app\jtmp` 已清理。

| APK 项目 | 值 |
|---|---|
| 文件 | `D:\记账app\工作台app\AI-Growth-OS\app\build\outputs\apk\debug\工作台_v1.5_debug.apk` |
| 大小 | 18,691,935 bytes |
| 修改时间 | 2026-08-27 18:15:03 |
| SHA-256 | `550FDD8594BBFBF002B1A4A900CB2F2E60D097D4D028863AB17FE491C49849B3` |
| applicationId / 类型 | `com.aigrowth.os` / debug |
| versionCode / versionName | 6 / 1.5 |

merged manifest 已确认包含 `allowBackup=true`、`QuickRecordNotificationService`、`android.permission.BIND_NOTIFICATION_LISTENER_SERVICE` 和 NotificationListenerService action。

## 六、尚未验证事项

以下事项当前不能标记为完成：

- 安装 APK，验证启动、Splash 跳过、Onboarding 跳过、抽屉和四屏 smoke test。
- 验证三页新增/编辑/删除/勾选，以及杀进程或重启后的 `simple_workbench` 持久化。
- 验证通知监听授权、付款通知唤起、金额、商户、去重、快捷记账开关和另开 Activity 体验。
- 验证工作台内嵌记账的聊天、手动记账、统计、记忆分类、CSV/日志导出。

## 七、接手优先级

### P0：设备与通知验收

1. 安装 debug APK，完成启动/跳过/抽屉/四屏 smoke test。
2. 完成三页操作及重启持久化验收。
3. 完成通知监听授权和付款通知全链路验收：金额、商户、去重、权限、开关。

### P1：业务验收与工程基线

1. 验证记账聊天、手动记账、统计和导出。
2. 决定是否将 `工作台app/AI-Growth-OS` 纳入 Git 并建立基线；当前不得假设已有 commit 或完整 diff。

### P2：发布与资料治理

1. 同步历史 README、overview、RELEASE_CHECKLIST，但先以本文和源码核对，避免旧说法回流。
2. 处理 release 签名、混淆、正式包和发布验收。

## 八、权限、数据与密钥

- Notification Listener 需用户在系统设置手动开启；设置页通过 `Settings.Secure.getString("enabled_notification_listeners")` 判定，并跳转系统授权页。
- 网络仅用于记账聊天 AI 兜底；三页纯本地，不依赖网络。
- 记账数据走 Room/既有仓储；三页用 `simple_workbench` Preferences DataStore，是有意的极简范围，不要顺手改成 Room 或 `PlanExecutor`。
- `AccountingApp.init(this)` 必须在宿主 Application `onCreate` 执行，否则 `getInstance()` 可能抛 `IllegalStateException`。
- DeepSeek API Key 仅运行时配置，不写入仓库、日志或文档。`b64:` + Base64 仅混淆，不是加密。

## 九、风险、红线与工程约束

1. 两套包名勿混：宿主 `com.aigrowth.os` ≠ 记账 `com.accounting.app`。
2. 禁止 `fallbackToDestructiveMigration()`，避免数据库升级丢数据。
3. 金额统一使用 `BigDecimal(amount.toString()).movePointRight(2).toLong()` 转分，禁止浮点或 `*100`。
4. 日志走 `AppLogger`，携带 `requestId` 与 `node` 并脱敏；新增流程保留入口、出口、状态变化和异常日志。
5. 保持 `allowBackup=true`；架构不得顺手引入 EventRouter、ActionBus、AgentCore 等过度分层。
6. `QuickRecordNotificationService` 可能另开 Activity，先设备验证再调整。
7. 中文路径可能令工具匹配失败；构建可使用短 TEMP 路径，不要破坏性清理源码或用户改动。
8. 顶层 Git 当前显示整个 `工作台app/AI-Growth-OS/` 及本交接文档为 untracked；不得声称有 Git 基线、提交或可靠 diff。

## 十、构建操作

```powershell
Set-Location 'D:\记账app\工作台app\AI-Growth-OS'
$env:TEMP='D:\记账app\jtmp'; $env:TMP='D:\记账app\jtmp'
.\gradlew.bat assembleDebug --no-daemon --console=plain
```

输出位于 `app\build\outputs\apk\debug\`，命名为 `工作台_v{versionName}_debug.apk`。构建后清理临时 `jtmp`，并核对 metadata 与 SHA-256；不要用破坏性 Git/文件系统命令清理用户改动。

## 十一、资料可信度矩阵

| 资料 | 当前可信度 | 使用规则 |
|---|---|---|
| 本文 v2.0 | 当前状态真源 | 与源码、APK 和设备验证结果同步 |
| 当前 `AI-Growth-OS` 源码 | 最高 | 以工作区现状为准；整体未纳入可靠 Git diff |
| debug APK / `output-metadata.json` | 高（构建时点） | 证明产物与版本，不等于设备行为已验收 |
| merged manifest 报告 | 高（构建时点） | 核对权限、服务和 `allowBackup` |
| `交接文档_工作台记账模块.md` | 历史参考 | 已被本文覆盖 |
| README、overview、RELEASE_CHECKLIST、旧计划 | 可能过时 | 修改前先与源码和构建产物核对 |

> 接收方从第七节 P0 开始。任何未完成的设备验证不得写成“已验收”。
