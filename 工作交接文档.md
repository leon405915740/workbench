# 工作交接文档 v4.3

本文件是 D:\项目 的 Current State 唯一权威来源。网页示例、旧文档、旧 APK、当前源码界面和历史提交均不得取代本文件的当前结论。冲突优先级：用户当前明确要求 → 本文档 → 实时源码与配置 → 自动化测试和构建产物 → 设备证据；网页和历史资料仅作参考。

## 1. 元数据与当前结论

| 项目 | 当前值 |
| --- | --- |
| 文档版本 | v4.3 |
| 更新日期 | 2026-08-29 |
| 项目唯一根目录 | D:\项目 |
| UI/交互/功能参考 | D:\项目\猫咪生活报\workbench-desktop.html |
| 远程仓库 | https://github.com/leon405915740/workbench.git |
| Git 状态 | 本地 main 与 origin/main 同步至 a3e45f7（工作树含响应式/字体放大/头像等未提交修复） |
| 当前状态 | v3.0 主体已实施并推送；响应式/横竖屏/字体放大/无障碍设备验证已完成；其余设备级验证项未完成 |

**当前结论**：v3.0（网页端工作台功能适配 Android + 完整保留记账功能/真实账单/设置数据）主体代码已实施并落地：常驻侧边栏、workbench.db 数据层、六大普通记录模块、首页/洞察/七天趋势/番茄钟、记账财务布局融合、账单与普通记录图片附件。网页仅作布局/交互/功能基线，用户当前要求高于网页示例；正式界面用中性模块名，不用猫系品牌。已在 MuMu 模拟器完成响应式/横竖屏/字体放大/无障碍触控验证并修复相应问题（见 8.3.1）；中文输入、付款通知、后台番茄钟仍需设备/自动化证据。

**已被否决的旧方案**（仅作历史背景，不得恢复）：旧五模块结构、各模块底部"打卡/记录/待办"三 Tab、抽屉式或临时展开主导航、全局快捷 Dock、以旧 APK 存在推断 v3.0 已完成。孤儿代码（feature/simple 模块、DashboardAccountingViewModel、ui/screens 下 HomeScreen/ModulePlaceholderScreen 占位屏）已删，不得重新引用。旧 APK 只是历史构建产物，不代表已验收。

## 2. 产品结构

**常驻一级导航**（左侧边栏固定顺序）：1 首页 / 2 今日计划 / 3 习惯打卡 / 4 阅读 / 5 运动 / 6 记账 / 7 随笔 / 8 剪报 / 9 洞察 / 10 设置（固定底部）。

**结构规则**：所有页面共享"常驻侧边栏 + 主区"外壳；手机窄屏也不改抽屉、底部三 Tab 或全局 Dock。不再为各模块设统一底部三 Tab，每模块在主区内自呈列表/统计/筛选/新增。冷启动固定进首页；返回或深链不无故切旧模块。用中性名称；网页提供基线，Android 平台化适配，不机械复制网页 CSS/悬停/浏览器存储。

## 3. 模块功能要点

各模块均提供领域适配的 CRUD、搜索、置顶、删除确认、统计（实现状态见第 7 节）。

- **首页**：时间/日期/问候/每日一句；今日聚焦跨模块置顶；"记运动/记打卡/记一笔/记想法"四快捷入口；今日概览环；本周习惯表；今日计划进度与关键待办；25/5 番茄钟（开始/暂停/继续/重置/完成次数/累计专注分钟持久化，后台继续计时，到时本地通知）；七天状态趋势；收支/阅读/运动摘要。
- **今日计划**：CRUD + 优先级/计划日期/备注/完成状态/置顶；今日完成进度与各优先级数量统计。
- **习惯打卡**：项目 CRUD + 当天打卡/取消/历史补打；连续天数/累计/当周完成/周追踪表；历史按日期保存，不用单布尔覆盖。
- **阅读与运动**：各自 CRUD + 搜索/置顶/删除确认/统计；保存当前值/目标值/单位/日期/备注并显示进度；增减后立即持久化，首页摘要与洞察同步。
- **随笔与剪报**：标题/正文/日期/标签/搜索/置顶/编辑/删除确认；随笔支持普通记录和引文，剪报支持来源/状态；卡片样式：标准/大图/引文；长正文/图片/标签/日期在列表与编辑中均保持可读可编辑。
- **全模块通用**：所有普通记录支持一张 App 私有本地图片（列表缩略图、详情可查看/更换/删除）；洞察页跨模块复盘；头像可从系统相册更换并存 App 私有空间；首页和模块页有明确加载/空/错误/无搜索结果状态；网页示例数据只用于首次播种非财务模块。

## 4. 移动端适配规则

- **布局**：窄屏侧边栏约 64dp、宽屏约 72dp，均常驻；窄栏优先图标+选中态，可用无障碍标签/长按提示/主区标题表达名称，不得改临时抽屉；手机主区单列或双列卡片，网页右侧统计栏窄屏移到正文下方；字体放大/横竖屏/小设备下不截断、不横向溢出、主操作可点。
- **新增编辑触控**：手机用底部弹层，宽屏用居中对话框；网页悬停改可见按钮/更多菜单/长按；删除始终需确认；触控目标/滚动/返回/焦点恢复按设备验证。
- **图片**：相册用系统 Photo Picker；仅用户主动选择拍摄时按需申请相机；普通记录和账单各最多一张 App 私有本地图片，数据库只存受控文件引用；删除/替换时同步清理私有文件；不做云同步。
- **中文输入与键盘**：组合态不重建搜索框/不提交半成品拼音/不跳光标/不清空；筛选在组合结束后触发；AI 记账/统计问答/搜索框/编辑弹层随键盘正确避让；聚焦输入框和主提交按钮始终可见可滚动；此项只在中文输入法+多屏尺寸+弹层+返回键场景均完成设备验证后才可标记修复。

## 5. 记账保留范围

**必须完整保留**：AI 对话记账与自然语言解析；手动新增收入/支出；金额/收支类型/分类/商户/日期时间/备注；编辑/删除/现有账单列表；收支/结余/笔数/分类占比/趋势统计及现有统计问答；分类记忆/分类映射管理/关键词规则/自动学习；付款通知监听/解析/快捷记账；API Key 配置入口及现有安全存储；CSV 导出与日志导出；全部现有真实账单、分类学习结果、映射、设置等用户数据。任何 UI 重构不得建第二套账本、绕开现有写入链路或覆盖真实数据。

**网页财务布局的 Android 适配**：顶部收支/结余/笔数概览；搜索/新建/AI 记账入口；账单列表按时间倒序保留编辑删除；分类占比/趋势/详细统计保留统计问答；首页"记一笔"和记账页按钮先弹 AI 录入，弹层中保留清晰手动填写入口；AI 解析确认后仍通过现有 Repository 写入原 Room 数据库，宿主只能通过现有 Repository 或 Bridge 访问记账数据；现有付款通知链路需适配新宿主导航与返回栈，未设备验证不得改写为已完成；记账模块不再用内部底部三 Tab。

**本轮唯一新增记账能力**：每笔账单最多附一张 App 私有本地凭证图片（留存小票/发票原图，不做 OCR，不上云，不改金额或分类解析逻辑）。

**明确排除**（除非用户重新授权）：预算、还款提醒、计划消费、账户与标签系统、记账日历、OCR、云端图片同步或云备份。

## 6. 数据与迁移（已实施）

- **workbench.db**：独立 Room 数据库，core/database/workbench 子包（WorkbenchDatabase.kt + WorkbenchDatabaseInitializer.kt + 各实体与 DAO + Hilt 模块 WorkbenchDatabaseModule.kt）；保存今日计划/习惯/阅读/运动/随笔/剪报/七天状态趋势/番茄钟状态统计/跨模块置顶/普通记录图片引用；首页和洞察只读各领域数据不建第二份业务真源。
- **一次性示例播种**：新工作台首次初始化时复制网页非财务演示数据用于各模块初始展示；不向真实记账库写网页示例账单；用数据库内明确初始化标记或等价事务机制确保只播种一次，升级/重启不重复插入，用户删除演示记录后不自动恢复。
- **记账 v6→v7 迁移（已实施）**：账单凭证图片通过显式 6→7 Migration 实现，expense 与 income 表各 ALTER TABLE ADD COLUMN attachmentPath TEXT；未改其他真实账单字段，未建独立附件表（等价一对一附件结构）；已禁用 fallbackToDestructiveMigration()，不采用任何清空用户数据的迁移捷径；金额按分存储，BigDecimal 完成字符串/元/分转换；宿主只能通过现有 Repository 或 Bridge 读写记账数据，不得直接持有 DAO 或另建账单表；完整迁移自动化测试已完成（见 7 表第 8 行、9 节第 2 项），覆盖 v6 有数据升 v7 / 无附件账单 / 附件增删换 / 升级后统计一致 / 失败回滚。
- **图片存储与生命周期**：账单凭证 AttachmentStore 把 content Uri 复制到私有目录 filesDir/bill_attachments/（时间戳+UUID 命名），save 返回绝对路径，delete 仅在文件父目录匹配该目录才删防误删；普通记录 WorkbenchImageStore 复制到 filesDir/workbench_images/，逻辑同上；账单 Repository 的 updateExpenseAttachment/updateIncomeAttachment 在路径变化时清理旧文件，deleteExpense/deleteIncome 删记录前先删附件；普通记录编辑弹窗通过 ImageAttachmentState.resolve() 统一管理选图/预览/删除草稿与旧文件清理，删记录时同步清理图片文件；图片路径只存受控私有路径字符串，不做 OCR、不上云。

## 7. 实施完成状态

状态分：已完成 / 部分验证（代码完成但缺完整设备/自动化证据）/ 待做。

| # | 事项 | 状态 | 证据 |
| --- | --- | --- | --- |
| 1 | 常驻侧边栏 + 响应式主区，冷启动进首页 | 已完成 | app/src/main/java/com/aigrowth/os/AIGrowthOSApp.kt 导航；WorkbenchChrome.kt 外壳 |
| 2 | workbench.db Room 数据层 + 一次性非财务示例播种 | 已完成 | core/database/workbench/ 下实体、DAO、Initializer、Hilt Module |
| 3 | 六大普通记录模块（计划/习惯/阅读/运动/随笔/剪报） | 已完成 | feature/plan、habit、reading、exercise、essay、clipping 各 ViewModel + Screen |
| 4 | 首页/洞察/七天状态趋势/后台番茄钟 | 部分验证 | feature/home、insight、statustrend、pomodoro；番茄钟用数据库存剩余秒+startedAt 恢复，AlarmManager+PomodoroReceiver 通知；后台省电限制下未真机验证 |
| 5 | 记账融合进网页财务布局（保留原 Repository+真实数据） | 已完成 | FinanceScreen.kt；MainViewModel 新增 financeRecords/financeSearchQuery 与 submitAiEntry；导航路由 "record?openAi={openAi}" |
| 6 | 账单 + 普通记录各一张 App 私有本地图片 | 部分验证 | 账单 EditRecordDialog+AttachmentStore；普通记录六模块 imageUri+WorkbenchImageStore/WorkbenchImage；MuMu 模拟器已验证选图/预览/删除 |
| 7 | 中文输入组合态与软键盘行为 | 待做 | 未完成设备验证（4 节全部要求） |
| 8 | 记账 v6→v7 迁移测试、全量记账回归、响应式与设备测试 | 部分验证 | 迁移自动化测试已完成（4/4 通过，见 AccountingMigrationTest.kt），设备 SM-S9110；响应式/横竖屏/字体放大/无障碍触控已在 MuMu 模拟器验证完成；全量记账回归未完成 |
| 9 | 构建正式 APK 并回写产物路径/大小/哈希/验收状态 | 已完成 | 工作台-1.8.1.apk 已构建并回写（见 8.5），MuMu 模拟器实测导航/记账设置开关正常 |

**已完成的关键修复**：
- 聊天列表 key 撞值崩溃：ChatScreen.kt 与 DashboardScreen.kt 的 LazyColumn 移除 `key = { it.timestamp }` 改默认位置 key；根因是消息 timestamp 取 System.currentTimeMillis() 毫秒值，连续同毫秒多条消息导致 "Key already used" 异常。已提交（a3e45f7）。
- 孤儿代码已删除：feature/simple 模块、DashboardAccountingViewModel.kt、ui/screens 下 HomeScreen.kt / ModulePlaceholderScreen.kt。
- 付款唤起记账卡片失败：根因为 Android 10+ 后台 startActivity 限制导致 NotificationListenerService 唤起被静默拦截；修复为 AndroidManifest 声明 SYSTEM_ALERT_WINDOW，QuickRecordNotificationService.launchQuickRecord 唤起前 Settings.canDrawOverlays 检查，SettingsScreen 新增"悬浮窗权限"入口行。

## 8. 当前工程事实与风险

**8.1 稳定工程事实**：唯一 Git 根目录 D:\项目；宿主包名 com.aigrowth.os，记账模块包名 com.accounting.app，资源/Manifest/数据库/导航边界不得混淆。Kotlin + Jetpack Compose 多模块；app 是宿主，feature/accounting 是现有记账模块。编译基线：compileSdk/targetSdk 34、minSdk 26、JVM 17（JDK 位于 D:\Android\jdk-17），JDK 21 会导致 kapt "tools.jar is absent"，必须用 JDK 17。现有记账 Room 数据库及 Repository 是真实账单唯一数据链路。推送走本机代理 127.0.0.1:7897（直连 github 不通，git 走 socks5h 代理可成功）。

**8.2 工作树与定向修改约束**：工作树已提交干净；仅剩两个非项目未跟踪项：猫咪生活报/（网页基线参考目录）与 codex-three-tier-task-routing-portable.zip.sha256，不纳入提交。禁止整体 reset、checkout、restore、clean 或用旧版本覆盖工作树。不得因某文件看似"旧"就删除；删除/迁移/停用必须有精确范围和验证证据。

**8.3 尚未验证的风险**：中文输入组合态与软键盘避让（4 节）；付款通知监听/权限/解析去重/快捷记账/返回栈在新导航下真机验证；后台番茄钟省电限制与进程重建后计时通知；Photo Picker 在真机/不同 Android 版本显示与私有图片进程重建恢复。（记账 v6→v7 完整自动化迁移测试已完成，见 7 表第 8 行与 9 节第 2 项。）

**8.3.1 响应式/横竖屏/字体放大/无障碍触控（已完成，MuMu 模拟器）**：物理 1344×2772、density 560、font_scale 1.0 竖屏基线正常；横屏旋转变正常；font_scale 1.3/2.0 下发现两处回归并已修复复验通过——① 周习惯名截断（"喝够 8 ..."），根因 WeeklyHabitTracker.kt 习惯标题 `maxLines=1`+`TextOverflow.Ellipsis`+固定 76dp，已改为允许换行；② 侧边栏底部项（剪报/洞察）在 font_scale 2.0 被裁剪不可达，根因侧边栏 Column 无滚动，已改为 `weight(1f, fill=false)+verticalScroll(rememberScrollState())`。另按用户要求将侧边栏头像放大至填满圆形背景框（size 由 (width-12dp)/2 调至 (width-12dp)*1f）。无障碍触控目标均 ≥48dp，侧边栏图标/按钮 content-desc 完整。证据见 D:\项目\screenshots\home_portrait_final.png、home_avatar_full.png、land_fs20_fix.png、ui_fs20_fix.xml、ui_fs20_habit.xml。

**8.4 安全和隐私**：DeepSeek Key 或任何 API Key 不得写入源码/Git/日志/交接文档/项目记忆/持久记忆；日志和导出不得泄露完整密钥/通知原文/账单敏感信息/无关个人数据；b64 等编码不是加密，编码后的密钥不得当可提交内容。

**8.5 当前构建产物**：debug APK：D:\项目\app\build\outputs\apk\debug\工作台-1.8.1.apk。大小 19,341,167 字节（18.44 MB），构建于 2026-08-29 19:21，SHA-256 为 09AEA6F14867F5E1B2F069DD577E3116D20147D775B4E673D522C765EC6306D5。applicationId 为 com.aigrowth.os，versionCode 11、versionName 1.8.1，variantName debug。本版本在 1.8.0 基础上完成设置重构（relocate-accounting-settings）：删除启动页；修复导航回退（个人中心进「全部设置」后再点头像可返回个人中心首页）；记账设置卡片迁入个人中心 ProfileScreen，含 4 个默认开启开关（自动学习分类/付款后唤起记账/通知监听权限/悬浮窗权限），默认收起；新增共享权限工具 PermissionUtils.kt（isNotificationAccessEnabled/isOverlayGranted/openNotificationAccessSettings/openOverlaySettings 四个 Context 扩展）；SettingsScreen 精简为仅「记忆与分类管理」入口 +「数据管理」卡片；修复导航 Bug「点首页仍显示个人中心」——navigateTo 移除 popUpTo(startDestinationId){saveState=true}+restoreState=true，改用 popUpTo(inclusive=false)+launchSingleTop，MuMu 实测 Profile→Home、Settings→Home 均正常。本版另新增 API 配置联通测试与 Key 清洗：个人中心「服务配置」卡片在「保存服务配置」上方新增「测试连接」按钮，保存前可调真实 DeepSeek 端点校验 Key/模型/网络，失败时给出可读提示（401/403/404/429/5xx/网络/不可见字符）；同时在 UserPreferences 写入与读取时统一剔除 C0/DEL 控制字符（修 `Unexpected char 0x16 at 7 in Authorization value`），并由 AppRepository.testConnection() 承载校验、ProfileScreen 展示结果。

**8.6 关键证据入口**：UI/交互/功能参考：D:\项目\猫咪生活报\workbench-desktop.html。宿主启动与导航：app/src/main/java/com/aigrowth/os/AIGrowthOSApp.kt 及相邻宿主源码。记账入口/页面/Repository/Room/通知/设置：feature/accounting/src/main/java/com/accounting/app/。记账迁移自动化测试：feature/accounting/src/androidTest/java/com/accounting/app/data/local/database/AccountingMigrationTest.kt（v6 手工构造库 + 有数据升 v7/无附件/附件增删换/统计一致/失败回滚，4/4 通过）。工作台数据层：core/database/src/main/java/com/aigrowth/os/core/database/workbench/。项目记忆路由：docs/project-memory-map.md。

## 9. 后续实施与验收顺序

主体已完成，剩余按序推进：

1. 完成中文输入组合态与软键盘行为（4 节）的设备验证与修复。
2. 完成记账 v6→v7 迁移自动化测试：✅ 已完成（4/4 通过，设备 SM-S9110）。测试文件 feature/accounting/src/androidTest/java/com/accounting/app/data/local/database/AccountingMigrationTest.kt，覆盖有数据升级/无附件账单/附件增删换/统计一致/失败回滚；为解锁源集编译，修复 CategorySelectorTest、CategoryManageScreenTabTest 无效 assertDoesNotExist 导入，删除引用已删组件的 CustomRangeDialogTest、MappingManageScreenFilterTest、MainViewModelToastTest。
3. 完成后台番茄钟在进程重建与系统省电限制下的计时与通知验证。
4. 完成付款通知链路在新导航/返回栈下的真机验证（含悬浮窗权限授权流程）。
5. 完成响应式/横竖屏/系统字体放大/无障碍触控验证：✅ 已完成（MuMu 模拟器，详见 8.3.1）。修复侧边栏滚动裁剪、周习惯名截断、头像过小后重新构建并通过。证据见 D:\项目\screenshots\。
6. 在最新提交上重新构建 debug APK，回写真实产物路径/大小/哈希/完成状态。

**分阶段验收**：导航（一级入口顺序正确、侧边栏常驻、设置固定底部、冷启动进首页，无底部三 Tab/抽屉/全局 Dock；侧边栏在系统字体放大下可滚动、所有项可达，已复验）；功能（网页功能清单逐项有 Android 等价实现；各模块 CRUD/搜索/置顶/删除确认/统计/图片行为可复现）；记账（真实账单与设置数据不变；AI/手动录入/编辑删除/统计问答/分类学习/通知快捷记账/API Key 配置/导出全部回归通过）；数据（workbench.db 一次性播种正确；记账 v6→v7 有数据迁移通过且统计一致）；输入（中文输入组合/搜索/AI 输入/统计问答/编辑弹层在多设备与键盘场景下无截断/遮挡/跳焦/异常空白）；图片（相册/拍摄/替换/删除/重启恢复/私有文件清理均通过；无 OCR 和云同步副作用）；构建（自动化测试/debug 构建/APK metadata/哈希/安装/启动/关键设备流程均有可定位证据）。

## 10. 当前交接点

v3.0 主体功能（导航外壳、workbench.db、六大普通模块、首页/洞察/状态趋势/番茄钟、记账财务布局、账单与普通记录图片附件）已实施、编译通过、在 MuMu 模拟器完成响应式/横竖屏/字体放大/无障碍触控设备验证，并已提交（d5371f9、a3e45f7）和推送至 origin/main（a3e45f7）。接手人从第 9 节开始排期，每阶段完成后用真实证据回写本文件。当前第 9 节第 2、5 项已完成。尚未提交的修复：侧边栏滚动、周习惯名换行、头像放大（见 8.3.1）。另新增（未提交）：API 配置联通测试与 Key 清洗——个人中心「服务配置」卡片新增「测试连接」按钮（保存前调真实 DeepSeek 端点校验 Key/模型/网络，失败给出可读提示），UserPreferences 写入/读取时统一剔除 C0/DEL 控制字符（修 `Unexpected char 0x16 at 7 in Authorization value`），见 8.5。
