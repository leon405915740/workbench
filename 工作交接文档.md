# 工作交接文档 v3.0

本文件是 D:\项目 的 Current State 唯一权威来源。它记录已经确认但尚待实施的产品基线、必须保留的工程能力、数据迁移边界和后续验收顺序。网页示例、旧文档、旧 APK、当前源码界面和历史提交均不得取代本文件的当前结论。

## 1. 文档元数据与当前结论

| 项目 | 当前值 |
| --- | --- |
| 文档版本 | v3.0 |
| 更新日期 | 2026-08-28 |
| 项目唯一根目录 | D:\项目 |
| UI、交互与功能参考 | D:\项目\猫咪生活报\workbench-desktop.html |
| 当前状态 | 需求已确认，尚未实施和设备验收 |
| 本轮交付边界 | 仅更新交接文档、项目记忆地图和授权的最小持久记忆备注；不改代码、配置、数据库或 APK |

### 1.1 当前结论

- v3.0 的目标是把网页端工作台的全部有效功能适配到 Android，同时完整保留现有记账功能、真实账单和设置数据。
- 产品采用“常驻侧边栏 + 主区”，冷启动固定进入首页。
- 网页文件提供布局、交互和功能基线，不是可直接交付的 Android 产物；用户当前明确要求始终优先于网页示例。
- 正式界面使用中性模块名称，不采用“喵生活报”或其他猫系名称作为正式品牌。
- 文档中所有 v3.0 功能均为待实施目标。未获得构建、自动化测试和设备证据之前，不得写成“已完成”“已修复”或“已验收”。

### 1.2 已被否决的旧方案

以下内容仅可作为历史背景，不得恢复为当前产品目标：

- 旧五模块结构。
- 各模块底部“打卡 / 记录 / 待办”三 Tab。
- 抽屉式或临时展开的主导航。
- 全局快捷 Dock。
- 以旧 APK 的存在推断 v3.0 已完成。

旧 APK 只是历史构建产物，不代表 v3.0 已实施、可交付或已通过真机验收。

## 2. 最终产品结构

### 2.1 常驻一级导航

左侧边栏始终展示，顺序固定为：

1. 首页
2. 今日计划
3. 习惯打卡
4. 阅读
5. 运动
6. 记账
7. 随笔
8. 剪报
9. 洞察
10. 设置，固定在侧边栏底部

### 2.2 结构规则

- 所有页面共享“常驻侧边栏 + 主区”外壳；手机窄屏也不得改回抽屉、底部三 Tab 或全局快捷 Dock。
- 不再为各模块设置统一的底部三 Tab。每个模块在主区内呈现自己的列表、统计、筛选和新增入口。
- 冷启动固定进入首页；从系统返回或深链返回时不得无故切换到旧模块。
- 使用“首页、今日计划、习惯打卡、阅读、运动、记账、随笔、剪报、洞察、设置”等中性名称。
- 网页负责提供布局、交互和功能基线；Android 应做平台化适配，不能机械复制网页 CSS、悬停逻辑或浏览器存储。
- 用户当前要求高于网页示例。网页中的品牌、示例文案、示例账单或与本基线冲突的交互均不得自动进入正式产品。

## 3. 网页端功能清单

以下能力均需适配到 Android；本节描述目标，不表示已经实现。

### 3.1 首页

- 显示当前时间、日期、问候语和每日一句。
- 今日聚焦支持跨模块置顶，并能进入对应记录或完成允许的快捷操作。
- 提供“记运动、记打卡、记一笔、记想法”四个快速入口。
- 显示今日概览环，汇总计划、习惯、阅读、运动等当日状态。
- 显示本周习惯追踪表。
- 显示今日计划完成进度和关键待办。
- 提供 25/5 番茄钟：开始、暂停、继续、重置、完成次数和累计专注分钟均需持久化；退到后台后继续计时，到时发送本地通知。
- 显示七天状态趋势，并提供清晰的状态记录入口和空态。
- 显示收支、阅读和运动摘要，点击后进入对应模块。

### 3.2 今日计划

- 支持新增、查看、编辑、完成、取消完成和删除。
- 支持优先级、计划日期、备注、完成状态和置顶。
- 显示今日完成进度、各优先级数量和相关统计。
- 搜索、筛选、删除确认和空态遵循全模块统一规则。

### 3.3 习惯打卡

- 支持习惯项目的新增、查看、编辑、停用和删除。
- 支持当天打卡、取消打卡、历史补打。
- 显示连续天数、累计天数、当周完成情况和周追踪表。
- 历史操作必须按日期保存，不得用一个布尔值覆盖全部历史。

### 3.4 阅读与运动

- 阅读和运动分别支持项目 CRUD、搜索、置顶、删除确认和统计。
- 每个项目保存当前值、目标值、单位、日期和备注，并显示进度。
- 阅读支持页数、章节或其他用户定义单位；运动支持分钟、次数、距离或其他用户定义单位。
- 增减进度后应立即持久化，并在首页摘要和洞察中同步反映。

### 3.5 随笔与剪报

- 随笔与剪报均支持标题、正文、日期、标签、搜索、置顶、编辑和删除确认。
- 随笔支持普通记录和引文表达；剪报支持来源或状态等适合剪藏的补充信息。
- 卡片布局提供标准、大图、引文三种样式。
- 长正文、图片、标签和日期在列表及编辑界面中都需保持可读和可编辑。

### 3.6 全模块通用能力

- 计划、习惯、阅读、运动、记账、随笔和剪报均需提供与领域适配的 CRUD、搜索、置顶、删除确认和统计。
- 所有普通记录支持一张 App 私有本地图片；列表提供缩略图，详情或编辑页可查看、更换和删除。
- 洞察页提供跨模块复盘，包括计划、习惯、阅读、运动、收支、状态趋势和番茄钟摘要。
- 头像可从系统相册更换并保存在 App 私有空间。
- 首页和模块页应有明确的加载、空、错误和无搜索结果状态。
- 网页中的示例数据只用于首次播种非财务模块，不作为真实用户记录或验收数据。

## 4. 移动端适配规则

### 4.1 布局与导航

- 手机窄屏侧边栏宽度约 64dp，宽屏约 72dp；两种规格均常驻展示。
- 窄栏优先显示图标和必要选中态，可通过无障碍标签、长按提示或主区标题表达名称，但不得改成临时抽屉。
- 手机主区按内容使用单列或双列卡片；桌面网页右侧统计栏在窄屏移到正文下方。
- 系统字体放大、横竖屏和较小设备下不得出现内容截断、横向溢出或无法点击的主要操作。

### 4.2 新增、编辑与触控

- 新增和编辑在手机使用底部弹层，宽屏使用居中对话框。
- 网页悬停操作改成可见按钮、更多菜单或长按操作；删除始终需要确认。
- 主要触控目标、滚动容器、返回行为和焦点恢复需按 Android 设备验证。

### 4.3 图片

- 从相册选择图片使用系统 Photo Picker。
- 只有用户主动选择拍摄时才按需申请相机权限。
- 普通记录和账单各支持最多一张 App 私有本地图片；数据库只保存受控文件引用和必要元数据。
- 删除或替换图片时同步清理不再引用的 App 私有文件；不实施云端图片同步。

### 4.4 中文输入与键盘

- 中文输入法处于组合态时不得重建搜索框、提交半成品拼音、跳动光标或清空输入。
- 搜索更新应保护输入法组合状态，必要时在组合结束后再触发筛选。
- AI 记账输入、统计问答、搜索框和所有编辑弹层必须随键盘正确避让。
- 聚焦输入框和主要提交按钮应始终可见并可滚动到达；不得出现输入框被遮挡、弹层上移失控或键盘上方异常大块空白。
- 该问题只有在中文输入法、不同屏幕尺寸、弹层和返回键场景均完成设备验证后，才可标记为修复。

## 5. 记账保留范围

### 5.1 必须完整保留的现有能力和数据

- AI 对话记账和自然语言解析。
- 手动新增收入或支出。
- 金额、收支类型、分类、商户、日期时间和备注。
- 编辑、删除和现有账单列表。
- 收入、支出、结余、笔数、分类占比、趋势等统计，以及现有统计问答。
- 分类记忆、分类映射管理、关键词规则和自动学习。
- 付款通知监听、通知解析和快捷记账。
- API Key 配置入口及现有安全存储方式。
- CSV 导出和日志导出。
- 全部现有真实账单、分类学习结果、映射、设置和其他用户数据。

任何 UI 重构都不得建立第二套账本、绕开现有写入链路或覆盖真实数据。

### 5.2 网页财务布局的 Android 适配

- 页面顶部显示收入、支出、结余和笔数概览。
- 提供搜索、新建和 AI 记账入口。
- 账单列表按时间倒序，保留编辑和删除。
- 显示分类占比、趋势和详细统计，并保留统计问答。
- 首页“记一笔”和记账页的记账按钮都先弹出 AI 录入；弹层中保留清晰的手动填写入口。
- AI 解析确认后继续通过现有 Repository 写入原 Room 数据库；宿主只能通过现有 Repository 或 Bridge 访问记账数据。
- 现有付款通知链路需要适配新的宿主导航与返回栈，未经设备验证不得改写为已完成。
- 记账模块不再使用内部底部三 Tab；现有能力在一个响应式主区中重新组织。

### 5.3 本轮唯一新增的记账能力

- 每笔账单最多附一张 App 私有本地凭证图片。
- 图片用于留存小票或发票原图，不做 OCR，不上传云端，不改变账单金额或分类解析逻辑。

### 5.4 明确排除

以下内容不属于当前 v3.0 实施范围，除非用户以后重新明确授权：

- 预算。
- 还款提醒。
- 计划消费。
- 账户与标签系统。
- 记账日历。
- OCR。
- 云端图片同步或云备份。

## 6. 数据与迁移计划

### 6.1 新工作台数据库

- 新建独立 Room 数据库 workbench.db，保存今日计划、习惯打卡、阅读、运动、随笔、剪报、七天状态趋势、番茄钟状态与统计、跨模块置顶状态及普通记录图片引用。
- workbench.db 首版从版本 1 开始，实体和迁移由实施阶段按模块边界设计；不得把新模块继续堆入旧简单模块 DataStore。
- 首页和洞察只读取各领域数据或聚合结果，不建立难以同步的第二份业务真源。

### 6.2 旧简单模块数据

- 旧健身、自媒体和英语 DataStore 不迁移、不展示，也不主动删除。
- 旧数据仅保留在原位置，防止未经授权的数据丢失；后续如需导入或清理，必须另行确认范围和迁移规则。

### 6.3 一次性示例播种

- 新工作台第一次初始化时复制网页中的非财务演示数据，用于计划、习惯、阅读、运动、随笔、剪报、状态趋势等模块的初始展示。
- 不向真实记账数据库写入网页示例账单。
- 演示数据只播种一次，使用数据库内明确的初始化标记或等价事务机制；升级、重启和重新进入页面不得重复插入。
- 用户删除演示记录后不得自动恢复。

### 6.4 记账数据库迁移

- 记账数据库当前真实版本记录为 v6。
- 在实施账单凭证图片时，通过显式 6 → 7 Migration 增加独立附件表或等价的一对一附件结构；不得借 UI 重构修改其他真实账单字段。
- 禁止使用 fallbackToDestructiveMigration() 或任何会清空用户数据的迁移捷径。
- 金额继续按分存储，并使用 BigDecimal 完成字符串、元和分之间的转换；禁止浮点金额和简单乘 100 的替代实现。
- 宿主只能通过现有 Repository 或 Bridge 读取和写入记账数据，不得直接持有 DAO 或另建账单表。
- 迁移测试必须至少覆盖 v6 有数据升级到 v7、无附件账单、添加/更换/删除附件、升级后统计一致和失败回滚。

## 7. 当前工程事实与风险

### 7.1 稳定工程事实

- 项目唯一 Git 根目录是 D:\项目。
- 宿主包名是 com.aigrowth.os，记账模块包名是 com.accounting.app；宿主与记账模块的资源、Manifest、数据库和导航边界不得混淆。
- 当前工程为 Kotlin、Jetpack Compose 多模块 Android 项目；app 是宿主，feature/accounting 是现有记账模块。
- 当前已知构建基线为 compileSdk/targetSdk 34、minSdk 26、JVM 17；真正实施时仍需以实时 Gradle 配置为准。
- 现有记账 Room 数据库及 Repository 是真实账单唯一数据链路。

### 7.2 工作树与定向修改约束

- 当前工作树已有用户未提交的源码、文档和未跟踪文件。
- 禁止整体 reset、checkout、restore、clean 或用旧版本覆盖工作树。
- 当前源码中的旧导航、旧底部结构和旧工作台组件可能与 v3.0 基线冲突；后续只能在查看实时 diff 后定向修改。
- 不得因为某文件看似“旧”就删除；删除、迁移或停用必须有精确范围和验证证据。

### 7.3 尚未验证的风险

- 输入框唤起、中文组合输入、软键盘避让和异常空白尚未完成设备验证。
- 付款通知监听、权限、解析去重、快捷记账和返回栈尚未在新导航中真机验证。
- Photo Picker、相机权限、App 私有图片生命周期和进程重建后的显示尚未验证。
- 后台番茄钟、持久统计、到时通知和系统省电限制下的行为尚未验证。
- 响应式侧边栏、横竖屏、系统字体放大和无障碍触控尚未验证。

### 7.4 安全和隐私

- DeepSeek Key 或任何 API Key 不得写入源码、Git、日志、交接文档、项目记忆地图或持久记忆。
- 日志和导出不得泄露完整密钥、通知原文、账单敏感信息或无关个人数据。
- b64 等编码不是加密；不得把编码后的密钥当成可提交内容。

### 7.5 历史构建结果

- 截至 2026-08-28 本次文档复核，历史文件 D:\项目\app\build\outputs\apk\debug\工作台_v1.5_debug.apk 存在。
- 复核到的文件大小为 18,860,004 字节，SHA-256 为 E468E3C67944F9A712F0BB29673AD3209B71463D6DEE8D9925A94F95A4C064C8。
- 该文件构建于 v3.0 实施之前，只能用于历史追踪，不得交付为 v3.0，也不得作为功能、迁移或设备验收证据。

### 7.6 关键证据入口

- UI、交互和功能参考：D:\项目\猫咪生活报\workbench-desktop.html。
- 宿主启动与导航：app/src/main/java/com/aigrowth/os/AIGrowthOSApp.kt 及相邻宿主源码。
- 记账入口、页面、Repository、Room、通知和设置：feature/accounting/src/main/java/com/accounting/app/。
- 项目记忆路由：docs/project-memory-map.md。
- 当前结论发生冲突时，优先级为：用户当前明确要求 → 本文档 → 实时源码与配置 → 自动化测试和构建产物 → 设备证据；网页和历史资料仅作参考。实现完成后，测试与设备证据用于回写本文档的完成状态。

## 8. 后续实施与验收顺序

### 8.1 实施顺序

1. 完成常驻侧边栏和响应式主区外壳，冷启动进入首页。
2. 建立 workbench.db Room 数据层与一次性非财务示例播种。
3. 适配今日计划、习惯打卡、阅读、运动、随笔和剪报。
4. 完成首页、洞察、七天状态趋势和可后台继续的番茄钟。
5. 将现有记账能力完整融合进网页财务布局，保持原 Repository 和真实数据。
6. 增加账单和普通记录各一张 App 私有本地图片。
7. 修复中文输入组合与软键盘行为。
8. 执行记账 v6 → v7 迁移测试、全量记账回归、响应式测试和设备测试。
9. 构建新 APK，完成安装和设备验收后，再回写真实产物路径、大小、哈希和完成状态。

### 8.2 分阶段验收

- 导航验收：所有一级入口顺序正确、侧边栏常驻、设置固定底部、冷启动进入首页，且不存在底部三 Tab、抽屉主导航和全局 Dock。
- 功能验收：网页功能清单逐项有 Android 等价实现；所有模块的 CRUD、搜索、置顶、删除确认、统计和图片行为可复现。
- 记账验收：真实账单与设置数据保持不变；AI、手动录入、编辑删除、统计问答、分类学习、通知快捷记账、API Key 配置和导出全部回归通过。
- 数据验收：workbench.db 一次性播种正确；旧简单 DataStore 未迁移、未展示、未删除；记账 v6 → v7 有数据迁移通过且统计一致。
- 输入验收：中文输入组合、搜索、AI 输入、统计问答和编辑弹层在多种设备及键盘场景下无截断、遮挡、跳焦或异常空白。
- 图片验收：相册、拍摄、替换、删除、重启恢复和私有文件清理均通过；无 OCR 和云同步副作用。
- 构建验收：自动化测试、debug 构建、APK metadata、哈希、安装、启动和关键设备流程均有可定位证据。

### 8.3 当前交接点

本轮只完成 v3.0 文档基线和项目记忆路由更新。上述 Android 代码、数据库迁移、输入框修复、图片能力、构建和设备验收仍未实施；接手人必须从第 8.1 节第 1 步开始，并在每一阶段完成后用真实证据更新本文档。
